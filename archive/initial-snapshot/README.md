# Initial public snapshot

This directory preserves the kernel screens, Mistral experiments, rejected
numerical-stability ideas, and raw evidence from the first public snapshot.
It is retained for provenance and is not part of the promoted Qwen3
reproduction path.

The code is historical and may depend on the original flat upload layout. Use
the repository history at commit `1583f3e` when reproducing that snapshot.
