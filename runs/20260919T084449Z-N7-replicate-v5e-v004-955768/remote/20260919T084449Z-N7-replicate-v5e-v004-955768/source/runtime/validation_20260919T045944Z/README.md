# Async wrapper offline validation

This check only parses scripts and manipulates local tar fixtures. It does not execute a remote worker, allocate a runtime, access the network, import JAX or run TPU code. The exact check and stdout are retained here. The parent agent owns commits.
